#include <iostream>
#include <cmath>

using namespace std;

int main()
{
    double result = floor(1.2);
    cout << result << endl;

    double power = pow(2, 3);
    cout << power;

    return 0;
}
